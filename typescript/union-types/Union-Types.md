We can set a variable to hold different types of values by using the `|` (pipe) symbol.

```
let result: number | string;

result = 10; // OK
result = 'Hi'; // also OK
result = false; // a boolean value, ERROR
```